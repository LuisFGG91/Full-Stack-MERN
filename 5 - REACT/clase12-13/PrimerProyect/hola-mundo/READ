

## Estructura de carpetas

/src
  /assets 
# Almacena recursos estáticos como imágenes, archivos de fuentes, y estilos globales.
  /components  
# Contiene componentes reutilizables que no están directamente asociados con una página específica.
  /contexts
# Guarda archivos relacionados con la implementación de contextos de React, si los estás utilizando.
  /hooks
# Almacena ganchos personalizados (custom hooks) que puedes reutilizar en diferentes partes de la aplicación.
  /pages
# Contiene componentes que representan páginas específicas de la aplicación.
  /services
# Guarda archivos relacionados con servicios, como llamadas a la API y configuraciones de servicios.
  /utils
# Almacena funciones y utilidades compartidas en toda la aplicación.
